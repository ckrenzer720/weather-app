/**
 * Weather App — Behavior
 * Purpose: form submit, Fetch API call, DOM updates, °C/°F toggle, loading/error states.
 * (No implementation yet; planning stage.)
 */

// Form submit → get search value → fetch weather → update DOM
// Unit toggle (°C / °F) → convert and re-render temp (and feels like if present)
// Error handling: city not found, network error
